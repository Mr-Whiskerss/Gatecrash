# Gatecrash
An API security scanner for the first hour of a pentest engagement. Point it at a Postman collection or OpenAPI spec plus a target host, give it two sets of credentials, and it hands back findings with the raw request/response pairs that prove them.
